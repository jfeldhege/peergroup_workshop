# fmt: skip file

average<-mean(feet/12 + inches,na.rm=T)
sqrt(x ^ 2 + y ^ 2) # Long comment that goes over the set length at 80 characters

base :: get  
mtcars$mpg == NA

a <- 1; b <- 1

c('a', 'b')

x = 5
